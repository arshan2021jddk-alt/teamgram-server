name: Build Teamgram Binaries for Windows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Fix gnet dependency for Windows
        shell: pwsh
        run: |
          go mod edit -replace github.com/teamgram/gnet/v2=github.com/panjf2000/gnet/v2@v2.6.0
          go mod tidy

      - name: Build all binaries
        shell: pwsh
        run: |
          $binDir = "bin"
          New-Item -ItemType Directory -Force -Path $binDir
          Get-ChildItem -Recurse -Filter main.go | ForEach-Object {
            $dir = $_.DirectoryName
            $serviceName = $dir | Split-Path -Leaf
            $output = Join-Path (Get-Location) "$binDir\$serviceName.exe"
            Write-Host "Building $serviceName..."
            Push-Location $dir
            go build -o $output
            Pop-Location
          }

      - name: Upload all binaries
        uses: actions/upload-artifact@v4
        with:
          name: teamgram-windows-all
          path: bin/
